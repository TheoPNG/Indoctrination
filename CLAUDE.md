# Indoctrination

Read [AGENTS.md](AGENTS.md) before editing. It covers the verification scripts
(there is no CI — those five scripts are the whole safety net), the
architecture invariants, the Unity traps that have already broken this game,
and the UI shapes that look odd on purpose.

`Docs/Editor Handoff Log.md` is the running record of what changed and why.
Add a dated entry at the end of a session.
