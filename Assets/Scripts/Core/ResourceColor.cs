namespace Indoctrination.Core
{
    /// <summary>
    /// The four resource colors players collect and spend. Matches the
    /// physical resource tokens described in the rules (red, green, blue, yellow).
    /// </summary>
    public enum ResourceColor
    {
        Red,
        Green,
        Blue,
        Yellow
    }
}
